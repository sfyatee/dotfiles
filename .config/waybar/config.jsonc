{
    "layer": "top",
    "position": "top",
    "height": 18,

    "modules-left": ["custom/cow-desks"],
    "modules-center": ["custom/cow-title"],
    "modules-right": ["cpu", "memory", "cpu#loadavg", "clock", "tray"],

    "cpu": {
        "format": "[CPU:{}]"
    },
    "memory": {
        "format": "[MEM:{used}]"
    },
    "cpu#loadavg": {
        "format": "[AVG:{load}]"
    },
    "custom/cow-desks": {
        "exec": "cowbar -w desks -o $WAYBAR_OUTPUT_NAME",
        "return-type": "json",
        "format": "{}",
        "tooltip": true
    },
    "custom/cow-title": {
        "exec": "cowbar -w title -o $WAYBAR_OUTPUT_NAME",
        "return-type": "json",
        "format": "{}",
        "tooltip": true,
        "max-length": 80
    },
    "clock": {
        "format": "[{:%b %d %H:%M:%S}]",
        "tooltip-format": "{:%A, %B %d, %Y}",
        "interval": 1
    }
}
