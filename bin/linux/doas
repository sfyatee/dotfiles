#!/usr/bin/env rc
u=''
o=()
while(! ~ $#* 0){
	switch($1){
	case -u
		u=$2
		shift 2
	case -n
		o=($o --no-ask-password)
		shift
	case -s
		o=($o --via-shell)
		shift
	case -i
		o=($o -i)
		shift
	case --
		shift
		break
	case *
		break
	}
}
if(! ~ $u '')
	o=(--user=$u $o)
exec run0 $o $*
