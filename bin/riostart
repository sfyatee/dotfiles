#!/usr/bin/env rc

# Services to run.
swaybg -m center -i $home/.local/share/backgrounds/grendel.png &

# Applications to run.
footclient aerc &
footclient comlink &

if(nc -z google.com 80 >[2]/dev/null){
	zen
}

load=()

if(test -f $home/acme.dump){
	load=(-l $home/acme.dump)
}

acme -f $font2 $load
