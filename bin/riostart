#!/bin/sh

# Services to run.
if [ `uname` != Linux ]; then
	fontsrv -m /mnt/font
	foot -s
fi

swaybg -m center -i ~/.local/share/backgrounds/grendel.png

# Applications to run.
footclient aerc
footclient comlink

if nc -z google.com 80 >/dev/null 2>&1; then
	zen
fi

acme -F $font2 -m /mnt/acme
